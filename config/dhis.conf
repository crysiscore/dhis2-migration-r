# Database connection URL
#connection.url = jdbc:postgresql:dhis2
connection.dialect = org.hibernate.dialect.PostgreSQLDialect
connection.driver_class = org.postgresql.Driver

connection.url= jdbc:postgresql://172.17.0.1:5432/dhis_v240

# Database username
connection.username =dhis_pepfar
# Database password
connection.password =@Dh15ccs2020

connection.schema = update


# ----------------------------------------------------------------------
# Server
# ----------------------------------------------------------------------

# Enable secure settings if deployed on HTTPS, default 'off', can be 'on'
server.https = on

# Server base URL
#server.base.url = https://server.com/


